@model AlterarSenhaVM

@{
    ViewBag.Title = "Alterar Senha";
    Layout = "_LayoutPublico";
}  
  
<form asp-action="AlterarSenha" method="post" class="form-signin">
    <p class='icone-autenticacao'> 🔑 </p>

    <h1 class="h3 mb-3 font-weight-normal"> @ViewBag.Title </h1>    
    
    @Html.Hidden("Id")

    <div asp-validation-summary="All" class="text-danger"></div>
    <input class="form-control" required maxlength="20" type="password" asp-for="NovaSenha"      placeholder="Nova Senha"           autofocus>
    <input class="form-control" required maxlength="20" type="password" asp-for="ConfirmarSenha" placeholder="Confirmar Nova Senha"          >

    <button class="btn btn-lg btn-primary btn-block" type="submit"> Salvar </button>
        
    <p class="mt-5 mb-3 text-muted">© App 2018</p>
</form>

@section scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}